# 🏆 Domain 1 Boss Fight — Introduction to Git & GitHub

> Score 12/15 or higher to defeat this boss! Honor system 🎖️
> Write your answers, then check against the key at the bottom.

---

### Q1. What type of version control system is Git?
- A) Centralized
- B) Distributed
- C) Local-only
- D) Cloud-based

### Q2. What does `git init` do?
- A) Clones a remote repository
- B) Creates a new branch
- C) Initializes a new Git repository
- D) Stages all files

### Q3. Which command stages changes for the next commit?
- A) `git commit`
- B) `git push`
- C) `git add`
- D) `git stage`

### Q4. What is the default branch name in new GitHub repositories?
- A) master
- B) develop
- C) main
- D) trunk

### Q5. What is a "remote" in Git?
- A) A local backup of your repo
- B) A version of your project hosted on the internet/network
- C) A branch that hasn't been merged
- D) A tag for releases

### Q6. Which GitHub plan includes SAML SSO?
- A) Free
- B) Pro
- C) Team
- D) Enterprise

### Q7. What command shows the commit history?
- A) `git status`
- B) `git log`
- C) `git diff`
- D) `git show`

### Q8. What does `git clone` do?
- A) Creates a new empty repo
- B) Downloads a copy of a remote repository
- C) Creates a new branch
- D) Merges two branches

### Q9. A merge conflict occurs when:
- A) Two branches have no common ancestor
- B) The same lines in a file were changed in both branches
- C) A push is rejected
- D) A branch is deleted

### Q10. What is a Git "commit"?
- A) A request to merge code
- B) A snapshot of your repository at a point in time
- C) A branch pointer
- D) A file upload

### Q11. GitHub is primarily:
- A) A Git GUI client
- B) A cloud-based platform for hosting Git repositories with collaboration tools
- C) A replacement for Git
- D) A CI/CD platform only

### Q12. What does `git pull` do?
- A) Pushes local changes to remote
- B) Fetches and merges remote changes into local branch
- C) Creates a pull request
- D) Pulls a Docker image

### Q13. Which file tells Git which files to ignore?
- A) .gitkeep
- B) .gitconfig
- C) .gitignore
- D) .gitmodules

### Q14. What is HEAD in Git?
- A) The first commit in a repo
- B) A pointer to the current branch/commit you're on
- C) The remote server
- D) The default merge strategy

### Q15. Organizations on GitHub can have:
- A) Only public repositories
- B) Multiple teams with different permissions
- C) Maximum 5 members
- D) Only one owner

---

<details>
<summary>🔑 Answer Key (click to reveal)</summary>

| Q | Answer | Explanation |
|---|--------|-------------|
| 1 | B | Git is a distributed VCS — every clone has the full history |
| 2 | C | `git init` creates a new `.git` directory |
| 3 | C | `git add` stages changes for commit |
| 4 | C | GitHub changed the default from `master` to `main` |
| 5 | B | A remote is a version of the repo hosted elsewhere |
| 6 | D | SAML SSO is an Enterprise feature |
| 7 | B | `git log` shows commit history |
| 8 | B | `git clone` copies a remote repo locally |
| 9 | B | Conflicts happen when the same lines are changed |
| 10 | B | A commit is a snapshot of the repo |
| 11 | B | GitHub is a cloud platform for Git with collaboration tools |
| 12 | B | `git pull` = `git fetch` + `git merge` |
| 13 | C | `.gitignore` specifies intentionally untracked files |
| 14 | B | HEAD points to the current branch tip |
| 15 | B | Orgs support multiple teams with granular permissions |

**Pass mark: 12/15 (80%)**

</details>
